import styled from 'styled-components'

export const Header = styled.h1({
    textTransform: 'uppercase',
    color: 'rgba(0, 0, 0, .5)'
})

export const ConvertionResult = styled(Header)({
    fontSize: '18px'
})